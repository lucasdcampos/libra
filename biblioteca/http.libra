erro("http.libra não foi implementado")

/*
Exemplo de uso:
var servidor = criar_servidor(3000)

servidor.get = funcao(req)
    se req.caminho == "/" entao
        retornar Res(200, "Olá, Mundo!")
    fim

    se req.caminho == "/teste" entao
        retornar Res(200, "Teste")
    fim
fim
*/