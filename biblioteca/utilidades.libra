// Biblioteca padrão da Libra
// Arquivo: utilidades.libra
// Libra é distribuída pela Licença MIT, veja https://github.com/linguagem-libra/libra

// Encerra o programa com uma mensagem de erro no console
funcao panico(mensagem: Texto, codigo: Int)
    erro(mensagem)
    sair(codigo)
fim